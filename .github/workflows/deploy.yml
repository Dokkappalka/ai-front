name: Deploy Frontend
on:
  push:
    branches:
      - production
jobs:
  build-and-deploy:
    runs-on: self-hosted
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          
      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./
          platforms: linux/amd64
          push: true
          provenance: false
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ai-hype-frontend:${{ github.sha }}
            ${{ secrets.DOCKERHUB_USERNAME }}/ai-hype-frontend:latest
          
      - name: Setup SSH Key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -H 213.176.116.131 >> ~/.ssh/known_hosts
          
      - name: Deploy to server
        env:
          DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
          GITHUB_SHA: ${{ github.sha }}
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.SERVER_USER }}@213.176.116.131 << EOF
            cd /opt/ai-hype
            export DOCKERHUB_USERNAME="${DOCKERHUB_USERNAME}"
            export FRONTEND_VERSION="${GITHUB_SHA}"
            export BACKEND_VERSION=\$(cat backend_version.txt 2>/dev/null || echo "latest")
            echo "${GITHUB_SHA}" > frontend_version.txt
            docker pull ${DOCKERHUB_USERNAME}/ai-hype-frontend:${GITHUB_SHA}
            docker compose up -d frontend
            docker image prune -f
          EOF

      - name: Copy nginx.conf to server
        run: |
          scp -i ~/.ssh/deploy_key nginx.conf ${{ secrets.SERVER_USER }}@213.176.116.131:/opt/ai-hype/frontend/ai-hype/nginx.conf

      - name: Restart nginx
        run: |
          ssh -i ~/.ssh/deploy_key ${{ secrets.SERVER_USER }}@213.176.116.131 'docker exec ai-hype-frontend-1 nginx -s reload'
          
      - name: Cleanup SSH Key
        if: always()
        run: |
          rm -f ~/.ssh/deploy_key